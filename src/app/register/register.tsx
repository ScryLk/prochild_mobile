import { View, Text } from "react-native";
import tailwindConfig from "tailwind.config";

export default function Register() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="font-sans text-primaryColor">
        Registro
      </Text>
    </View>
  );
}